node_modules
dist
.git
.gitignore
Dockerfile*
docker-compose*.yml
README.md
npm-debug.log*
.DS_Store
.env
.env.*
# Exclude uploads (359M) — these are volume-only on the running container.
# (Boss 2026-08-20: was being baked into image as part of wp-content/ COPY,
# bloating the wp-content layer to 1.47GB and stalling GHCR uploads.)
wp-content/uploads/
